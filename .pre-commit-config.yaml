# Secret scanning on every commit. Install hooks: pre-commit install
# Skip once: SKIP=gitleaks git commit -m "message"
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
